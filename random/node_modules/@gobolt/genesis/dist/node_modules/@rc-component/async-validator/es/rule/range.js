import { format as i } from "../util.js";
var y = function(e, s, n, r, u) {
  var g = typeof e.len == "number", l = typeof e.min == "number", t = typeof e.max == "number", a = s, m = null, o = typeof s == "number", f = typeof s == "string", p = Array.isArray(s);
  if (o ? m = "number" : f ? m = "string" : p && (m = "array"), !m) return !1;
  p && (a = s.length), f && (a = s.replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, "_").length), g ? a !== e.len && r.push(i(u.messages[m].len, e.fullField, e.len)) : l && !t && a < e.min ? r.push(i(u.messages[m].min, e.fullField, e.min)) : t && !l && a > e.max ? r.push(i(u.messages[m].max, e.fullField, e.max)) : l && t && (a < e.min || a > e.max) && r.push(i(u.messages[m].range, e.fullField, e.min, e.max));
};
export {
  y as default
};
