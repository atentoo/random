import c from "react";
import m from "../../theme/useToken.js";
import { devUseWarning as f } from "../warning.js";
import v from "../zindexContext.js";
const e = 100, E = 1e3, a = { Modal: e, Drawer: e, Popover: e, Popconfirm: e, Tooltip: e, Tour: e, FloatButton: e }, x = { SelectLike: 50, Dropdown: 50, DatePicker: 50, Menu: 50, ImagePreview: 1 }, N = (t, o) => {
  const [, p] = m(), s = c.useContext(v), d = t in a;
  let r;
  if (o !== void 0) r = [o, o];
  else {
    let n = s ?? 0;
    n += d ? (s ? 0 : p.zIndexPopupBase) + a[t] : x[t], r = [s === void 0 ? o : n, n];
  }
  if (process.env.NODE_ENV !== "production") {
    const n = f(t), i = p.zIndexPopupBase + 1100, u = r[0] || 0;
    process.env.NODE_ENV !== "production" && n(o !== void 0 || u <= i, "usage", "`zIndex` is over design token `zIndexPopupBase` too much. It may cause unexpected override.");
  }
  return r;
};
export {
  E as CONTAINER_MAX_OFFSET,
  x as consumerBaseZIndexOffset,
  a as containerBaseZIndexOffset,
  N as useZIndex
};
