function h(e) {
  return (e + 8) / e;
}
function f(e) {
  const o = Array.from({ length: 10 }).map((t, r) => {
    const a = r - 1, n = e * Math.pow(Math.E, a / 5), i = r > 1 ? Math.floor(n) : Math.ceil(n);
    return 2 * Math.floor(i / 2);
  });
  return o[1] = e, o.map((t) => ({ size: t, lineHeight: h(t) }));
}
export {
  f as default,
  h as getLineHeight
};
