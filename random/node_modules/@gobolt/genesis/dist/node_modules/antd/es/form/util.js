const u = ["parentNode"];
function d(n) {
  return n === void 0 || n === !1 ? [] : Array.isArray(n) ? n : [n];
}
function c(n, e) {
  if (!n.length) return;
  const t = n.join("_");
  return e ? `${e}_${t}` : u.includes(t) ? `form_item_${t}` : t;
}
function l(n, e, t, a, o, i) {
  let r = a;
  return i !== void 0 ? r = i : t.validating ? r = "validating" : n.length ? r = "error" : e.length ? r = "warning" : (t.touched || o && t.validated) && (r = "success"), r;
}
export {
  c as getFieldId,
  l as getStatus,
  d as toArray
};
