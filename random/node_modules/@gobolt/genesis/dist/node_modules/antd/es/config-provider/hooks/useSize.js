import n from "react";
import r from "../SizeContext.js";
const i = (t) => {
  const o = n.useContext(r);
  return n.useMemo(() => t ? typeof t == "string" ? t ?? o : t instanceof Function ? t(o) : o : o, [t, o]);
};
export {
  i as default
};
