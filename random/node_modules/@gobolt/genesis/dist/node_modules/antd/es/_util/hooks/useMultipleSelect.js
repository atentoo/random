import { useState as i, useCallback as d } from "react";
function M(h) {
  const [u, n] = i(null);
  return [d((t, p, l) => {
    const s = u ?? t, m = Math.min(s || 0, t), o = Math.max(s || 0, t), r = p.slice(m, o + 1).map((a) => h(a)), c = r.some((a) => !l.has(a)), e = [];
    return r.forEach((a) => {
      c ? (l.has(a) || e.push(a), l.add(a)) : (l.delete(a), e.push(a));
    }), n(c ? o : null), e;
  }, [u]), (t) => {
    n(t);
  }];
}
export {
  M as default
};
