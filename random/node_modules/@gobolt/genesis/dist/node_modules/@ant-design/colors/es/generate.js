import { FastColor as m } from "../../fast-color/es/FastColor.js";
var M = [{ index: 7, amount: 15 }, { index: 6, amount: 25 }, { index: 5, amount: 30 }, { index: 5, amount: 45 }, { index: 5, amount: 65 }, { index: 5, amount: 85 }, { index: 4, amount: 90 }, { index: 3, amount: 95 }, { index: 2, amount: 97 }, { index: 1, amount: 98 }];
function i(t, a, u) {
  var n;
  return (n = Math.round(t.h) >= 60 && Math.round(t.h) <= 240 ? u ? Math.round(t.h) - 2 * a : Math.round(t.h) + 2 * a : u ? Math.round(t.h) + 2 * a : Math.round(t.h) - 2 * a) < 0 ? n += 360 : n >= 360 && (n -= 360), n;
}
function d(t, a, u) {
  return t.h === 0 && t.s === 0 ? t.s : ((n = u ? t.s - 0.16 * a : a === 4 ? t.s + 0.16 : t.s + 0.05 * a) > 1 && (n = 1), u && a === 5 && n > 0.1 && (n = 0.1), n < 0.06 && (n = 0.06), Math.round(100 * n) / 100);
  var n;
}
function s(t, a, u) {
  var n;
  return n = u ? t.v + 0.05 * a : t.v - 0.15 * a, n = Math.max(0, Math.min(1, n)), Math.round(100 * n) / 100;
}
function p(t) {
  for (var a = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, u = [], n = new m(t), e = n.toHsv(), r = 5; r > 0; r -= 1) {
    var x = new m({ h: i(e, r, !0), s: d(e, r, !0), v: s(e, r, !0) });
    u.push(x);
  }
  u.push(n);
  for (var o = 1; o <= 4; o += 1) {
    var v = new m({ h: i(e, o), s: d(e, o), v: s(e, o) });
    u.push(v);
  }
  return a.theme === "dark" ? M.map(function(h) {
    var g = h.index, f = h.amount;
    return new m(a.backgroundColor || "#141414").mix(u[g], f).toHexString();
  }) : u.map(function(h) {
    return h.toHexString();
  });
}
export {
  p as default
};
