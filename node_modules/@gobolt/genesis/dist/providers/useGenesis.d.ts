import { ToastContextType } from '@gobolt/genesis/providers/ToastProvider';
import { BREAKPOINTS } from '@gobolt/genesis/constants/index';
export declare const useGenesis: () => {
    theme: any;
    breakpoint: keyof typeof BREAKPOINTS;
    toast: ToastContextType;
};
