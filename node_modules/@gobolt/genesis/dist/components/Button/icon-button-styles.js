import k from "styled-components";
const z = k.button`
  ${({ theme: t, $themeType: i, $state: c, size: a, $isSelected: d }) => ((b, u, $, p, g) => `
    font-family: 'Inter', sans-serif;
    ${(({ colors: r, sizing: e, borderRadius: l, components: x }, o, s, h, f) => `
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: ${r[o][s].color};
      font-size: ${e.Size4}px;
      line-height: ${e.Size6}px;
      letter-spacing: 0;
      font-weight: 400;
      border-width: 1px;
      cursor: pointer;
      width: auto; !important;
      height: auto; !important;

      background-color:${f ? r[o].hover.backgroundColor : r[o][s].backgroundColor};
      padding: ${((C, n) => C === "small" ? `${n.Size1}px ${n.Size1}px` : `${n.Size2}px ${n.Size2}px`)(h, e)};

      box-shadow: ${o === "utility" ? "0px 1px 2px 0px #00000026" : "none"};

      border-radius: ${l.BorderRadiusMd}px;
      gap: ${x.button.gap}px;
      border-style: solid;
      border-color: ${r[o][s].borderColor};
      
      transition: all 0.2s ease-in-out;

      &:hover {
        color: ${r[o].hover.color};
        background-color: ${r[o].hover.backgroundColor};
        border-color: ${r[o].hover.borderColor};
      }

      &:active {
        color: ${r[o].pressed.color};
        background-color: ${r[o].pressed.backgroundColor};
        border-color: ${r[o].pressed.borderColor};
      }

      &:focus-visible {
        outline: none;
        color: ${r[o].focussed.color};
        background-color: ${r[o].focussed.backgroundColor};
        box-shadow: 0 0 0 1px #fff, 0 0 0 4px ${r[o].focussed.ringColor};
        border-radius: ${l.BorderRadiusMd}px;
        border-color: ${r[o].focussed.borderColor};
        transition: box-shadow 0.2s ease-in-out;
      }

      &:disabled {
        color: ${r[o].disabled.color};
        background-color: ${r[o].disabled.backgroundColor};
        border-color: ${r[o].disabled.borderColor};
        cursor: not-allowed;
      }
    `)(b, u, $, p, g)}
`)(t, i, c, a, d)}
`;
export {
  z as Button
};
