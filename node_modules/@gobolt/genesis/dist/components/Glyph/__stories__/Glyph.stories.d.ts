import { Meta, StoryObj } from '@storybook/react';
import { default as Glyph } from '../Glyph';
declare const meta: Meta<typeof Glyph>;
export default meta;
type Story = StoryObj<typeof Glyph>;
export declare const GlyphStory: Story;
