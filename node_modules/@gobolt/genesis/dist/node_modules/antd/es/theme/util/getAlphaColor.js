import { FastColor as o } from "../../../../@ant-design/fast-color/es/FastColor.js";
function n(t) {
  return t >= 0 && t <= 255;
}
function w(t, l) {
  const { r: a, g: e, b: g, a: s } = new o(t).toRgb();
  if (s < 1) return t;
  const { r: f, g: c, b: d } = new o(l).toRgb();
  for (let r = 0.01; r <= 1; r += 0.01) {
    const u = Math.round((a - f * (1 - r)) / r), b = Math.round((e - c * (1 - r)) / r), i = Math.round((g - d * (1 - r)) / r);
    if (n(u) && n(b) && n(i)) return new o({ r: u, g: b, b: i, a: Math.round(100 * r) / 100 }).toRgbString();
  }
  return new o({ r: a, g: e, b: g, a: 1 }).toRgbString();
}
export {
  w as default
};
