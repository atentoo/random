import a from "./genRadius.js";
function r(i) {
  const { motionUnit: o, motionBase: t, borderRadius: n, lineWidth: e } = i;
  return Object.assign({ motionDurationFast: `${(t + o).toFixed(1)}s`, motionDurationMid: `${(t + 2 * o).toFixed(1)}s`, motionDurationSlow: `${(t + 3 * o).toFixed(1)}s`, lineWidthBold: e + 1 }, a(n));
}
export {
  r as default
};
