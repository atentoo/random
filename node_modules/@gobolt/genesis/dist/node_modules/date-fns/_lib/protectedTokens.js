const i = /^D+$/, f = /^Y+$/, d = ["D", "DD", "YY", "YYYY"];
function u(t) {
  return i.test(t);
}
function Y(t) {
  return f.test(t);
}
function h(t, n, r) {
  const o = function(e, s, c) {
    const a = e[0] === "Y" ? "years" : "days of the month";
    return `Use \`${e.toLowerCase()}\` instead of \`${e}\` (in \`${s}\`) for formatting ${a} to the input \`${c}\`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md`;
  }(t, n, r);
  if (console.warn(o), d.includes(t)) throw new RangeError(o);
}
export {
  u as isProtectedDayOfYearToken,
  Y as isProtectedWeekYearToken,
  h as warnOrThrowProtectedError
};
