import { compareAsc as r } from "./compareAsc.js";
import { differenceInCalendarMonths as h } from "./differenceInCalendarMonths.js";
import { isLastDayOfMonth as M } from "./isLastDayOfMonth.js";
import { toDate as f } from "./toDate.js";
function u(n, i) {
  const t = f(n), e = f(i), s = r(t, e), o = Math.abs(h(t, e));
  let a;
  if (o < 1) a = 0;
  else {
    t.getMonth() === 1 && t.getDate() > 27 && t.setDate(30), t.setMonth(t.getMonth() - s * o);
    let m = r(t, e) === -s;
    M(f(n)) && o === 1 && r(n, e) === 1 && (m = !1), a = s * (o - Number(m));
  }
  return a === 0 ? 0 : a;
}
export {
  u as default,
  u as differenceInMonths
};
