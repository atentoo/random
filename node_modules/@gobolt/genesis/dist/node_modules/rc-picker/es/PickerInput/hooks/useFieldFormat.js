import u from "../../../../@babel/runtime/helpers/esm/typeof.js";
import * as i from "react";
import { getRowFormat as p, toArray as s } from "../../utils/miscUtil.js";
function l(t, e, f) {
  return i.useMemo(function() {
    var n = p(t, e, f), m = s(n), o = m[0], a = u(o) === "object" && o.type === "mask" ? o.format : null;
    return [m.map(function(r) {
      return typeof r == "string" || typeof r == "function" ? r : r.format;
    }), a];
  }, [t, e, f]);
}
export {
  l as useFieldFormat
};
